/**
 * Shared Supabase client for the SPA. Lovable also generates
 * `src/integrations/supabase/client.ts` — re-exported here for a stable import path.
 */
export { supabase } from '../integrations/supabase/client';
